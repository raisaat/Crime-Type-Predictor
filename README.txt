The code (code.py) is written in Python and can be run in a Python3 environment. It has to be run in the same directory as data.csv.

The following Python libraries are required to run code.py:
numpy
matplotlib
mlxtend
math
sklearn
pandas
itertools